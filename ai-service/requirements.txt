# ── Core ───────────────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1

# ── Database ───────────────────────────────────────────────────────────────────
motor==3.3.2           # Async MongoDB driver
pymongo==4.6.3         # Pinned: motor 3.3.2 is incompatible with pymongo>=4.7
redis==5.0.3           # Redis client

# ── HTTP ───────────────────────────────────────────────────────────────────────
httpx==0.27.0          # Async HTTP client (for calling Express API)

# ── AI / ML (Phase 4) ─────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.8
langchain-google-genai>=1.0.0
langchain-community>=0.2.0
openai>=1.30.0
google-generativeai>=0.7.0
sentence-transformers>=2.7.0    # Local embeddings (all-MiniLM-L6-v2, free)
pypdf>=4.1.0                    # PDF text extraction
python-docx>=1.1.0              # DOCX text extraction

# ── Dev ────────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pytest==8.1.1
pytest-asyncio==0.23.6
httpx==0.27.0           # needed for TestClient
