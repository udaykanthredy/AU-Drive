# ── Core ───────────────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1

# ── Database ───────────────────────────────────────────────────────────────────
motor==3.3.2           # Async MongoDB driver
redis==5.0.3           # Redis client

# ── HTTP ───────────────────────────────────────────────────────────────────────
httpx==0.27.0          # Async HTTP client (for calling Express API)

# ── AI / ML (Phase 2+) ────────────────────────────────────────────────────────
# Uncomment when implementing Phase 2:
# langchain==0.1.16
# langchain-openai==0.1.3
# langchain-community==0.0.36
# openai==1.14.3
# sentence-transformers==2.7.0   # Local embeddings (all-MiniLM-L6-v2)
# pypdf==4.1.0                   # PDF text extraction
# python-docx==1.1.0             # DOCX text extraction

# ── Dev ────────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pytest==8.1.1
pytest-asyncio==0.23.6
httpx==0.27.0           # needed for TestClient
